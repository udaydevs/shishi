{
    "builds": [{
        "src": "ecom/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb","includeFiles": ["requirements.txt"] }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "ecom/wsgi.py"
        }
    ]
}